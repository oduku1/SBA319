<!DOCTYPE html>
<html>
<head><title>My Anime List</title></head>
<body>
  <h1>📺 My Anime List</h1>

  <form action="/anime" method="POST">
    <input type="hidden" name="userId" value="<%= userId %>">
    <input type="text" name="title" placeholder="Title" required>
    <input type="text" name="genre" placeholder="Genre (comma separated)">
    <input type="number" name="episodes" placeholder="Episodes">
    <input type="number" name="rating" placeholder="Rating (0–10)">
    <select name="status">
      <option>Plan to Watch</option>
      <option>Watching</option>
      <option>Completed</option>
      <option>Dropped</option>
    </select>
    <button type="submit">Add Anime</button>
  </form>

  <ul>
    <% list.forEach(anime => { %>
      <li>
        <b><%= anime.title %></b> — <%= anime.status %> (<%= anime.rating %>/10)
      </li>
    <% }) %>
  </ul>

  <a href="/auth/login">Logout</a>
</body>
</html>
